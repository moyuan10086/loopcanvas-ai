# Infinite Canvas 二次开发说明

## 来源与版本

- 上游项目：[basketikun/infinite-canvas](https://github.com/basketikun/infinite-canvas)
- 上游许可证：GNU Affero General Public License v3.0（AGPL-3.0）
- 本分支基线：上游 `v0.8.2`，提交 `0c4288b8325c95a8bdca76e93737d07ffbc55f7e`
- 本次归档版本：`v0.8.2-moyuan.1`
- 归档日期：2026-08-02
- 对比时上游状态：`v0.12.1`，提交 `ea0414e88cffa6b522cc13c0613b3c8085983a53`

本仓库是基于上游项目的个人二次开发版本，不是原作者的官方发行版。所有上游版权、作者信息、来源说明和 AGPL-3.0 许可证均予以保留。本分支对外提供网络服务或分发时，应继续遵守 AGPL-3.0，并向对应用户提供完整源代码。

## 本分支的主要改造

### 1. 面向真实生产的多渠道模型接入

- 将通用 OpenAI 兼容调用扩展为可维护的多渠道配置，适配 DeepKey、ToAPIs、Apilio、APIMart、MiniMax、RunningHub、ComfyUI 与 ModelScope 等渠道。
- 对图片、视频、音频和文本模型分别处理同步、异步任务、轮询、结果解析、代理、超时和供应商错误。
- 适配 GPT Image 2、Grok Imagine、Flux、Kling、Seedance 2.0、MiniMax TTS/Music 等具体协议差异。

### 2. 模型价格与成本对比

- 支持为模型维护人民币价格、自定义覆盖价格和完整模型 ID。
- 支持 DeepKey、APIMart、ToAPIs 等定价来源抓取，并按渠道配置美元、额度与人民币换算比例。
- 在模型选择器中展示渠道、能力、价格和价格缺失状态，便于按成本选择模型。

### 3. 循环节点与电商卖点图工作流

- 新增独立循环节点，支持串行、4 worker 有界并发、停止、失败汇总和多步骤级联执行。
- 支持编号提示词批量粘贴、`{{i}}` / `{{r}}` / `{{n}}` 变量、固定参考图多选、轮换素材和直接文本并发生图。
- 新增“卖点图”模式：一组商品参考图配合编号卖点清单，逐条生成多张商品卖点图，并保证每轮只发送对应卖点。
- 结果按轮次和步骤自动布局，每轮从循环节点建立独立分支，减少重叠和错误引用。

### 4. 参考图可追踪与编辑可靠性

- 区分源图、显式 `@` 引用、隐式连接和循环固定参考图，避免换装、卖点图等多图编辑丢失主体或重复发送参考图。
- 图片节点增加跨画布稳定的 `IMG-` 索引，并保存 `referenceNodeIds`、`referenceLabels` 和存储键，便于识别引用来源和可靠重试。
- 非幂等生成请求避免盲目自动重提；网络中断会提示可能已经计费，降低重复扣费风险。

### 5. 本地后端持久化

- 画布、节点媒体、渠道配置和 API 调用统计同步保存到本地 Canvas Agent；IndexedDB 保留为离线缓存。
- 增加修订冲突保护、旧任务响应隔离、失败重试和 Agent 重连合并，减少刷新、换浏览器或多标签页导致的数据丢失。
- RunningHub 异步任务保存任务 ID 和扣费通道，刷新后可继续轮询。

### 6. 创作工具和工作流

- RunningHub SeedVR2 / 工作流超分，区分消费级会员与企业级共享 Key。
- ComfyUI 工作流导入、ModelScope 异步图片模型、扩图、矢量化、视频抽帧、视角调整、遮罩编辑和 AI 图片快捷功能。
- API 调用统计面板与完整统计页，显示调用量、成功率、耗时和失败原因。

### 7. UI 与加载性能

- 保留原主题色，统一节点卡片、输入控件、Focus Ring、阴影、圆角、悬停和选中状态。
- 增加 React 路由级懒加载，配置弹窗和 CodeMirror 按需加载，并通过 Rollup 拆分稳定缓存块。
- 生产构建中初始入口约 `255.32 kB`，gzip 约 `84.92 kB`；CodeMirror 编辑器不再随首页首屏加载。

## 与当前上游 v0.12.1 的边界

上游在 `v0.9.0` 至 `v0.12.1` 重点增强了资产面板、配置导入导出、Codex 模型与推理强度、权限审批、任务进度、历史会话、诊断日志和新版 Agent 交互。本分支目前没有完整合并这 111 个后续提交。

因此，本分支的核心价值是面向多供应商与批量视觉生产的垂直增强；上游新版 Agent 能力应作为后续选择性合并事项，而不是本分支已经完成的功能。

## 参考实现说明

部分 ComfyUI、ModelScope、扩图和 RunningHub 工作流的产品交互参考了 `hero8152/Infinite-Canvas` 的公开功能设计。由于其许可证与本项目 AGPL-3.0 不兼容，本分支采用独立实现，没有复制其源码。相关说明也保留在变更日志中。
