# CHANGELOG

## Unreleased

- [优化] 前端页面改为 React 路由级懒加载，配置弹窗及 CodeMirror 编辑器按打开状态加载，首页不再同步载入画布、视频、素材和配置页；Vite 生产构建通过 Rollup 将 React、Ant Design、CodeMirror、Motion 和 Streamdown 拆为稳定缓存块，降低非目标页面的初始 JavaScript 负担。
- [优化] 重构循环节点与卖点图模式的视觉层级：生成规则、卖点清单和商品参考素材改为独立轻量卡片，合并左侧连接图与固定参考图的重复展示，增加固定底部执行栏；新建循环节点默认尺寸调整为 460×520，并设置专属最小尺寸，避免完整表单被压缩到不可读状态。
- [修复] 画布反向连线改为严格连接“源节点右侧输出口 → 目标节点左侧输入口”的单段连续曲线，不再使用生硬的外围折线；循环加载和运行时会清理“循环输入/固定参考图直连本循环结果”的重复边，只保留循环轮次分支与真实多步骤链路。
- [修复] 卖点图模式默认按顺序执行，并将旧版自动设为并行的卖点循环一次性迁移为串行，避免 DeepKey 等慢速图片编辑渠道被多请求同时占用后在约 120 秒集体断开；用户仍可主动切换为同时运行。
- [新增] 循环节点增加“卖点图”任务模式：支持直接在循环节点内填写编号卖点清单并自动同步生成张数，也可从右侧空白生成模板或上游文本导入；第 N 轮只发送第 N 条卖点，并在每轮固定复用全部商品参考图，运行前校验商品图与卖点数量。
- [修复] 外部服务通过 URL 参数导入 API Key 与 Base URL 时，改为按 Base URL 更新已有渠道或新增独立渠道，不再覆盖渠道列表第一项；重复导入保持幂等，缺少或非法 Base URL 时不修改配置。
- [修复] Windows 一键启动脚本检查到 Codex 尚未注册 `infinite-canvas` MCP 时不再被 stderr 提前中断，能够继续自动注册并完成启动。
- [新增] 循环节点“附加输入”区域增加固定参考图多选、多图上传和逐张移除，每轮在轮换素材之外复用整组固定图；逐轮提示词可插入 `@固定图`，固定图与 `{{i}}`、`{{r}}`、`{{n}}` 参数会使用蓝色高亮。
- [修复] 直接编辑已有图片时始终保留当前图片为主参考，显式 `@` 或隐式连接图片只作为附加参考，避免换装等多图编辑只发送衣服而丢失模特源图。
- [修复] 新浏览器 Profile 可从已授权的本机网页来源安全发现 Canvas Agent 连接 Token，全站 Agent 自动连接后立即拉取并合并后端画布，画布库侧边栏不再只显示当前浏览器 IndexedDB 缓存。
- [修复] 画布与节点媒体改为变更后立即向本地 Agent 排队保存，先落盘节点项目再同步媒体；新快照会跳过旧任务的媒体同步并禁止旧响应回写，修订冲突也会保留已完成媒体，避免生成成功后刷新变成空节点。写入失败时保留最新快照并自动重试，Agent 重连会强制拉取、合并和回写，后端项目不再写入失效的 `blob:` 地址。图片节点新增跨画布固定的 `IMG-` 索引，生成记录同时保存文件键、引用节点与固定索引标签，便于辨认和稳定重试。
- [优化] 参考 Shadcn、Linear 与 Vercel 优化控件和主页质感：输入控件增加 1px 边框与柔和 Focus Ring，主页移除冗余装饰并统一高对比文字、微弱光感、双层阴影和作品卡状态，同时保留原主题色。
- [修复] 循环节点将滚动内容与底部执行栏拆分布局，底部参数和按钮不再覆盖提示词或堆积在节点边缘，开关标题也不再被强制截断。
- [优化] 在保留原有主题色的前提下，参考 Coze / Arco Design 统一画布的布局密度、16px 节点圆角、8px 控件圆角、文字层级，以及悬停、选中、连接和缩放状态。
- [新增] 循环节点未连接右侧处理步骤时可直接使用每轮文字指令和当前默认图片模型串行或并发生图，每轮固定生成一张。
- [新增] 画布项目、节点媒体与 API 调用统计自动同步保存到本地 Canvas Agent，浏览器 IndexedDB 保留为离线缓存并支持旧画布首次合并迁移。
- [修复] 画布与配置首次连接 Agent 时改为按连接实例合并，画布保存增加修订号冲突保护，避免晚连接、切换 Agent 或多标签页覆盖已有数据。
- [修复] RunningHub 恢复轮询改为按节点和任务独立管理，多个排队任务不会因列表变化互相取消；CORS 图片改由本地 Agent 代下载落盘。
- [修复] 提示词库过滤上游 Language/Raycast 等徽章并改取真实作品封面，同时对 Twitter 图片增加代理回退与稳定占位。
- [修复] 循环节点失败时立即显示具体原因，顶部“重试”会重新执行循环，不再误走普通图片重试逻辑。
- [新增] 画布底部工具栏增加 API 统计面板，实时显示今日调用量、成功率、平均耗时、失败数与最近请求，并可进入完整统计页。
- [修复] APIMart GPT Image 2 改按官方异步协议提交比例与分辨率并轮询任务结果，移除不支持的同步返回参数，文生图和图生图统一读取任务图片 URL。
- [修复] 图片节点存在连入媒体时只使用直接连入媒体作为参考，没有连入媒体时才使用节点自身图片，避免单条图片连线被重复计为两张参考图。
- [优化] 循环生成结果按“步骤列 × 轮次行”自动布局，结合节点尺寸计算列宽和行高，避免多轮多步骤结果重叠、错列或连线交叉。
- [修复] 循环执行不再把右侧步骤模板自身作为参考图或产物父节点；每轮从循环节点建立独立分支，后续步骤只连接并引用本轮上一步结果。
- [新增] 循环节点支持将编号列表整段粘贴为逐轮指令并自动同步轮数；单张输入图会在每轮固定复用，多张图仍按顺序取用。
- [调整] 循环流程不再自动重提可能已经提交成功的非幂等请求，避免网络错误导致重复扣费；单轮失败后继续执行后续轮次，并在节点内汇总成功数与具体失败轮次。
- [修复] 循环节点的“同时运行”模式改为固定 4 个 worker 的有界并发，避免 100 轮任务同时冲击供应商；串行模式保持逐轮执行。
- [修复] 循环提示词变量按钮会在当前光标或选区位置插入，不再固定追加到提示词末尾；插入后继续保持文本框焦点。
- [优化] 循环节点按调度器职责重排为模式、输入、提示词变量和执行区，缩减空白与分隔；循环控制节点不再显示生成耗时，耗时只保留在实际生成结果上。
- [新增] “模型”页支持为每个渠道模型填写人民币自定义价格，价格会覆盖内置规则并同步显示在模型选择器；清空可恢复自动价格，配置自动持久化并可显式同步到本地 Agent 后端。
- [优化] 模型价格管理默认折叠，不再展开整个渠道的空白价格输入列表；展开后通过可搜索模型下拉一次编辑一个模型，并稳定显示模型名、能力、当前价格和抓取操作。
- [修复] 编辑已有模型价格时，输入框直接显示当前内置或已抓取价格并在聚焦时全选，用户可在基础价格上直接修改；自定义价格单独标识，清空后恢复基础价格。
- [优化] 新增抓价表单明确区分“渠道”和“完整模型 ID（不是链接）”，修复价格示例中反引号直接显示的问题，并将按钮文案改为“添加模型并抓价 / 打开定价来源”。
- [优化] 已有模型价格编辑区新增可全选的完整模型 ID 文本框与复制按钮，长 ID 不再依赖下拉框截断文本，便于直接粘贴到接口测试或渠道后台。
- [修复] 模型抓价请求增加并发保护并复用同一条结果提示；旧版 Agent 缺少抓价路由时改为提示重启一键启动脚本，不再连续堆叠多个 `not found`。
- [修复] APIMart 定价抓取支持渠道代理、`HTTPS_PROXY` / `HTTP_PROXY` 与 Windows 系统代理自动回退；本机系统代理场景下 Agent 可直接抓取，不再因浏览器代理与 Node 网络隔离而失败。
- [修复] ToAPIs 恢复自动价格抓取，改用官方 `/api/pricing` 模型目录接口，不读取浏览器 Cookie；按官网中文界面的 `$1 = ¥7` 换算并支持模型精确匹配。
- [新增] 渠道定价支持单独配置美元/额度兑人民币比例；ToAPIs 默认按官方中文界面 `$1 = ¥7`，APIMart 默认按 `$1 = ¥6.7801`，DeepKey 使用人民币原价，抓取结果会记录实际换算比例。
- [新增] 渠道支持单独填写“定价抓取代理 URL”，只影响本地 Agent 访问定价页；留空时自动使用环境变量或 Windows 系统代理，不影响模型 API 调用。
- [新增] 渠道可配置定价来源 URL，模型页支持填写完整模型名后添加并抓价，或对已有模型单独抓取；DeepKey 读取公开 `/api/pricing`，APIMart 解析公开定价页结构化数据并换算人民币，ToAPIs 登录页会明确提示手填。
- [安全] 在线价格抓取仅允许 DeepKey、APIMart、ToAPIs 官方 HTTPS 域名，不读取或保存浏览器 Cookie；需要登录或代理时提供“打开来源”和清晰错误提示。
- [修复] 将 RunningHub `2053691179258134529`（LTX2.3 视频生视频）从图片/超分模型迁移为视频模型，旧配置也会自动纠正能力分类。
- [调整] 画布节点生成面板恢复跟随节点显示，并通过右下角手柄支持等比例调整大小；节点创建菜单增加可调整尺寸和内部滚动。
- [修复] 生成面板等比例缩放增加最小可用尺寸，避免缩小时把提示词、模型和生成按钮裁掉；切换节点后恢复完整面板。
- [新增] RunningHub 渠道预置参考项目的 9 个工作流 ID（新增 3 个高清放大工作流），并支持从渠道编辑器一键导入；工作流生成/编辑请求按渠道默认扣费方式传递企业级-共享 Key。
- [调整] RunningHub 参考项目工作流改为可勾选同步，取消勾选的预置工作流会从当前渠道模型库移除，手动添加的其他模型保留。
- [调整] 参考项目的 RunningHub 图片工作流接入 AI 超分工作流选择器，统一上传参考图、提示词、分辨率、随机种子和异步结果解析；LTX2.3 去水印工作流按视频模型接入并支持视频参考上传。
- [修复] RunningHub 显式选择企业级-共享时不再回退到消费级-会员 Key，缺少共享 Key 会直接提示配置错误。
- [修复] RunningHub 完成态兼容 `fileUrl` 的多层/字符串响应，并在结果下载跨域时保留远程图片 URL，避免任务已完成但图片节点为空。
- [修复] 视频抽帧弹窗关闭浏览器原生视频控件，统一使用自定义播放按钮和时间轴；拖动定位与实际导出时间保持同步。
- [调整] 循环节点按参考项目完善为内嵌操作卡，支持多提示词、图片批次、起始计数、串行/多轮并发和整条下游链路一键运行，并把每步实际产物传给下一步。
- [优化] 循环提示词改用短变量 `{{i}}`（素材序号）、`{{r}}`（当前轮次）、`{{n}}`（总轮数），按钮同时显示中文含义与当前值；旧的中文变量继续兼容并自动显示为新格式。
- [调整] 循环节点默认开启“步骤提示词”，可沿用下游图片、视频、音频或文本节点原提示词重跑整条处理链；关闭后才只使用循环追加指令和显式输入。
- [新增] 画布生成节点自动记录本次请求耗时，并在节点左上角以半透明绿色标签显示；媒体自身播放时长继续使用独立字段。
- [优化] 视频节点移除浏览器原生控件和播放进度条，改为悬停显示播放时间、静音与全屏操作；空白处直接拖拽即可框选多个节点，多选时不再弹出单节点生成面板。

+ [新增] 图片节点“AI 超分”接入 RunningHub SeedVR2 工作流，支持 2K/4K 目标、比例保持、任务取消、结果子节点及失败重试；渠道配置可填工作流 ID 并添加工作流模型。
+ [修复] 画布生成上下文对面板提示词与连入文本做内容去重，配置节点只回写原始输入，避免相同提示词在生成和重试时重复累加。
+ [调整] 画布生成新增“隐式参考”开关；关闭时只有显式 `@` 资源引用会携带连接媒体，开启时自动携带连接图片、视频或音频，专用图片编辑入口仍会使用源图。
+ [修复] 图片批量/循环生成的每一轮复用同一份显式或隐式参考资源，不会因展开批量结果而丢失参考图。
+ [新增] 视频节点新增“提取帧”工具，可按时间定位视频画面并生成相连的图片节点，图片可继续参与 `@` 引用、批量生成和 AI 超分。
+ [新增] 新增独立“循环”节点：支持连接上游参考与下游生成节点，配置轮数、串行/并行、动态提示词和隐式参考，并可独立停止循环。
+ [优化] 扩图预览对齐参考项目，支持拖动四边和右下角控制点实时调整画布范围，并显示目标分辨率。
+ [修复] 扩图边距超出最大画布尺寸时自动按四边比例收敛到 3840px 上限，统一预览与生成尺寸计算，避免提交时再次报错。
+ [调整] RunningHub 超分添加工作流前先按官方 `getJsonApiFormat` 接口验证工作流 ID；界面明确官方未提供账号工作流列表接口，避免把无效工作流加入模型库。
+ [新增] RunningHub 渠道支持分别配置消费级-会员 API Key 和企业级-共享 API Key，AI 超分可选择扣费方式并在重试时保留原选择。
+ [调整] RunningHub 渠道新增默认扣费方式配置，支持自动优先消费级-会员、固定消费级-会员或固定企业级-共享。
+ [修复] RunningHub 超分保存异步任务 ID，刷新页面后按原扣费方式恢复排队任务轮询；旧的刷新中断节点迁移为可重试状态。
+ [修复] ToAPIs GPT Image 与 Flux 图片模型适配异步任务响应，提交后轮询任务图片结果，不再把任务 ID 当成空图片响应。
+ [调整] 模型下拉补充 APIMart、Apilio 与 ToAPIs 已启用图片模型的人民币价格、渠道价格缺失标识与分组差异说明。
+ [修复] 图片编辑请求设为 180 秒超时，DeepKey 等上游无响应时节点显示可重试的超时原因，不再永久停留在生成中。
+ [修复] OpenAI 兼容图片编辑会先无扣费探测本地 Canvas Agent 的代理能力：新版 Agent 可用时由其安全转发，未连接、离线或旧版时无感继续浏览器直连，生成不再依赖 Agent。代理仅允许后端配置中的渠道和 `/images/edits`，不向网页暴露渠道密钥；已向代理提交后的不确定失败不会自动改走直连，避免重复扣费。
+ [修复] 循环卖点图结果重试会依据保存的 `generationType` 与 `references` 恢复原商品参考图，继续调用 `/images/edits`；不再因为提示词没有显式 `@` 而误走无参考图的 `/images/generations`。本地代理等待上限延长到 300 秒，覆盖 DeepKey 后台已观察到的 176–199 秒非流式图片任务。
+ [调整] 图片请求在无 HTTP 响应或等待超时时不再建议立即重试，明确提示渠道后台可能仍在生成并计费，避免 DeepKey 已成功但前端未取回结果时重复扣费。
+ [修复] ToAPIs Flux 图片编辑不再发送 multipart 字符串形式的 `n`，避免服务端整数反序列化失败。
+ [修复] APIMart Grok Imagine 图片模型改用 API 子域名与异步任务轮询协议，按宽高比提交请求，正确读取任务结果中数组形式的图片 URL；浏览器跨域下载结果失败时直接保留远程图片，不再把已完成任务误报为 `Failed to fetch`。
+ [修复] 图片接口透传供应商错误码，便于识别 Apilio Grok 上游无图片等渠道故障；模型下拉同步显示 Apilio Grok 和 DeepKey 最新人民币价格。
+ [调整] 渠道与模型配置新增本地 Canvas Agent 后端持久化，启动时优先恢复后端配置，并兼容迁移旧版浏览器配置，避免渠道和 API Key 因浏览器存储键变化丢失。
+ [修复] 修正 Apimart Grok Imagine 模型能力归类，并按 ToAPIs Kling 版本与帧模式发送正确的参考图片字段，避免图片未进入视频请求。
+ [修复] 一键启动脚本改为打开最近使用的画布，避免每次启动重复新建画布；网页已运行时重启 Agent 会保留当前浏览器会话，不再额外打开画布。
+ [新增] 图片节点新增「扩图」工具，支持四方向独立扩展、方向预设、实时输出尺寸预览和自定义补画要求；扩展画布通过现有图片编辑模型生成关联的新节点，不覆盖原图。
+ [新增] 渠道配置新增 ModelScope 魔塔预设，内置常用免费图像与文本模型；图像生成和编辑使用 ModelScope 异步任务提交、状态轮询与失败解析。
+ [新增] 渠道配置新增 ComfyUI 预设，可导入 API 格式工作流 JSON；新增 RunningHub 预设，可通过工作流 ID 添加 SeedVR2 超分工作流并在画布中调用。
+ [说明] ComfyUI、ModelScope 与扩图交互参考了 [hero8152/Infinite-Canvas](https://github.com/hero8152/Infinite-Canvas) 的公开功能设计；因其自定义非商业许可证与本项目 AGPL-3.0 不兼容，本项目采用独立实现，未复制对方源码。

+ [同步] 合并上游 `v0.8.2` 插件系统与模型调用脚本能力，并保留下方本地增强功能。

## v0.8.2 - 2026-07-16

+ [新增] 图像设置新增「透明背景」开关,开启后生成无背景的透明图像。
+ [修复] 画布节点提示词输入框补上悬停文本光标。
+ [优化] 画布节点输入区域移除灰色底色与边框、美化样式。

## v0.8.1 - 2026-07-16

+ [新增] 插件 SDK 扩展:AI 生成能力、面板控制能力。
+ [优化] 3D 全景(1.1.0)支持上传与 AI 生成并升级查看器;
+ [优化] HTML 节点(1.2.0)迁移到统一交互开关;
+ [优化] 便利贴(1.1.0)可拖动移动、自选颜色、移除资源角标与衍生功能;
+ [优化] SVG 节点(1.1.0)透明背景融入画布、可拖动、去除默认值;
+ [优化] 画布节点渲染性能:memo 化节点回调,交互时不再全量重渲染;
+ [修复] 修复 Markdown 节点在点击/移动视角时重复渲染导致图片反复请求的问题;
+ [修复] 修复插件版本号显示不更新，插件面板新增可升级绿点提醒。

## v0.8.0 - 2026-07-15

+ [新增] 画布节点插件系统:支持通过 URL 动态安装/启用/更新/卸载远程节点插件。
+ [新增] 插件开发 SDK,可用 TypeScript 开发画布节点插件。
+ [新增] 新增 Markdown、SVG、HTML、3D 全景、便利贴等示例插件。
+ [新增] 官方插件注册表:节点插件面板可从项目仓库读取官方插件列表并一键安装。
+ [新增] 在画布右上角工具栏新增「节点插件」入口。
+ [新增] 支持自定义生图/视频接口调用方式以适配不同中转站。

## v0.7.1 - 2026-07-15

+ [修复] 修复页面白屏报错的问题。
## 本地增强（未发布）

+ [修复] 按 gpt-best 可灵官方格式适配 `kling-video-v3`：创建任务改用 `/kling/v1/videos/image2video` JSON 接口，首尾帧分别传入 `image` / `image_tail`，Base64 图片移除 Data URL 前缀，时长归一为接口支持的 5 秒或 10 秒，并通过专用查询接口等待结果。
+ [新增] 完整适配 ToAPIs Seedance 2 多模态参考生视频：图片、视频、音频分别使用 `reference_image`、`reference_video`、`reference_audio` 角色，本地参考视频会先上传到 `/uploads/videos` 再提交生成任务。
+ [调整] 图片视角调整改为优先使用 `qwen-image-edit-plus`，Plus 不可用时才回退到 Apilio 渠道的 `qwen-image-edit-max-2026-01-16`，不再自动选择通用 `qwen-image-edit-max`。
+ [新增] 视角调整弹窗新增图片编辑模型选择器，可在自动推荐结果与其他已配置图片模型之间手动切换本次调用模型。
+ [修复] 修复画布 `@` 资源引用标签被拼入生成提示词的问题：配置编辑器和普通提示框中的图片、首尾帧、视频与音频仅作为结构化参考传递，文本引用直接展开正文且不再附加“文本1”等编号；重试时也会重新解析原始引用。
+ [修复] 按 ToAPIs Seedance 2 文档适配 `/videos/generations` 创建与轮询协议，本地/Base64 首尾帧会先通过 `/uploads/images` 自动上传，再使用 `image_with_roles` 传递公开 URL 并解析 `result.data`；同时保留火山 Agent Plan 的原有接口格式，视频请求会提前拦截相对或无效 Base URL。
+ [修复] ToAPIs 视频任务创建后先等待 5 秒再开始轮询，处理中每 10 秒查询一次；任务短暂未落库、限流、网络抖动或服务端错误时继续等待，不再让已提交任务提前进入失败状态。
+ [优化] 视角调整的水平旋转范围扩展为 `-180°~180°`，支持侧后方和完整背面视角，并同步适配 Qwen 角度提示词。
+ [修复] 修正视角调整把自然语言编辑误当成精确相机参数的问题：连续角度按正面、前侧 3/4、侧面、后侧 3/4、背面五类语义视角执行，提示词要求旋转整个场景相机并禁止裁切/二维透视变形。
+ [修复] 修复视角调整旋转滑杆触发提示浮层 Portal 无限更新并导致页面崩溃的问题，同时减少立方体拖动时的无效状态刷新。
+ [优化] 提示词库明确标识上游原始提示词并保留 Comment / 说明，加入我的素材时同步保存为备注；再次加入同一提示词会更新已有素材而非重复创建。
+ [修复] 修复从素材库导入提示词时忽略素材标题、空内容仍创建节点，以及已选空文本节点未被复用而产生重复节点的问题。
+ [新增] 新增 API 调用统计页面，本地记录图片、视频、音频和文本生成请求的渠道、模型、耗时、状态及输入输出摘要，并支持筛选和清空。
+ [优化] 区分图片节点 AI 编辑与 AI 快捷功能图标，避免相邻操作图标重复。
+ [新增] 画布支持多选图片后批量连接到同一个目标节点，并自动跳过重复连线。
+ [新增] 图片节点新增可拖拽视角调整，支持旋转、倾斜、推进和广角镜头控制；自动优先使用已配置的 Qwen-Image-Edit-Plus，并支持 Fal Qwen-Image-Edit-2511 生成关联的新视角图片。
+ [新增] 局部遮罩编辑新增矩形选框，支持拖动框选和 Shift 锁定正方形。
+ [优化] 图片节点快捷工具较多时自动收起按钮文字并限制工具栏宽度，减少对画布节点的遮挡。
+ [优化] 模型多选下拉在连续勾选时保留搜索关键词，并按包含渠道名的显示标签匹配结果。
+ [修复] 区分渠道完整模型库与实际启用模型，避免渠道模型菜单全部显示为已勾选，并提供直达模型选择入口。
+ [新增] 音频节点适配 MiniMax 同步语音合成与 Music 2.6 音乐生成接口，MiniMax 渠道可一键补充 TTS / Music 模型并支持音色、歌词、自动歌词和纯音乐模式。
+ [新增] 图片节点新增 AI 快捷功能，支持多机位九宫格、电影级光影校正、角色三视图和前后画面推演。
+ [新增] 视频生成新增参考图、首帧和首尾帧输入模式，并按图片顺序传递帧角色。
+ [优化] 首尾帧模式突出显示首帧 / 尾帧标识，并支持一键交换两帧顺序。
+ [优化] 图片节点工具栏明确提供 AI 编辑入口，直接使用原图执行提示词编辑。
+ [修复] 修复画布节点提示面板中模型、参数与生成按钮在长模型名下发生重叠的问题。
+ [修复] 修复提示词库无法解析 HTML 图片标签，导致部分封面显示为空的问题。

## v0.7.0 - 2026-07-14

+ [新增] Agent 对话消息改用 streamdown 流式渲染，提升Markdown 内容展示效果。
+ [新增] Agent 新增画布、工作台、提示词库和素材等站点级工具。
+ [新增] Agent 面板改为全站常驻右侧栏，开关时同步推动顶栏和页面内容。
+ [新增] Agent 新增 `site_navigate` 工具，支持页面跳转。
+ [新增] Agent 对话运行中支持一键停止，中断当前 Codex turn。
+ [新增] 画布节点支持统一维护名称字段，默认显示在节点上方，并可直接双击名称编辑。
+ [新增] 画布新增组节点，支持节点拖入/拆出分组、拖拽高亮吸附和移动组时带动子节点。
+ [新增] 画布空白区域支持双击打开节点选择菜单，并在点击位置创建节点。
+ [调整] Codex 会话改为站点级连续线程，跨页面和跨画布保持同一上下文。
+ [调整] 移除仅前端调用 OpenAI responses 接口，统一走 MCP + 本地 Codex 链路。
+ [调整] 画布节点顶部工具条改为点击选中节点后显示，避免鼠标经过节点时频繁弹出。
+ [优化] 本地 Agent 连接说明明确区分插件 / 手动 MCP 才会增加 Codex token 消耗。
+ [优化] 优化本地 Agent 连接说明，区分 Codex 插件启动和直接运行 Agent 两种方式。
+ [修复] 修复 Gemini 格式生图时因内置比例列表触发误报导致生成失败的问题。

## v0.6.0 - 2026-07-09

+ [新增] 新增Codex App插件支持。
+ [新增] 配置与用户偏好新增独立页面和 Codex 连接配置 Tab。
+ [新增] 新增GitHub Pages 前端静态站点发布 workflow。
+ [新增] 图片切图支持等分线直接拖拽调整，并可新增、删除和重置横向 / 纵向切图线。
+ [调整] Docker 运行镜像改为 nginx 静态托管。
+ [调整] 移除网站Agent模式，专注于连接Codex Agent操作画布
+ [修复] 修复生图工作台重试成功结果刷新后丢失的问题。
+ [修复] 修复 Gemini 调用格式生图未传递尺寸比例配置的问题。
+ [修复] 修复前端 TypeScript 构建报错。
+ [修复] 修复画布生成配置切换文本/视频/音频模式时模型仍显示为生图模型的问题。
+ [修复] 兼容中转站视频任务直接返回视频 URL 且没有 `/content` 接口的情况，并优化失败原因展示。

## v0.5.0 - 2026-07-05

+ [新增] 渠道兼容Gemini格式。
+ [调整] 前端从 Next.js 迁移到 Vite，项目改为静态前端构建。
+ [调整] 移除已 404 的 EvoLinkAI 提示词来源。

## v0.4.0 - 2026-06-16

+ [新增] 新增网页版Agent Loop模式。
+ [新增] 支持Vercel一键部署。
+ [调整] 移除后端，项目定位为个人画布工具。

## v0.3.0 - 2026-06-15

+ [新增] 新增canvas-agent通过codex操作画布。

## v0.2.5 - 2026-06-08

+ [新增] 新增图片切图功能。
+ [新增] 支持webdav同步数据。
+ [修复] 修复画布文字节点错误问题。

## v0.2.4 - 2026-06-04

+ [新增] 新增图片反推提示词功能。

## v0.2.3 - 2026-06-04

+ [新增] 新增图片蒙版局部修改功能。
+ [优化] 优化配置节点@图片功能。

## v0.2.2 - 2026-06-04

+ [新增] 新增图片放大工具。
+ [优化] 优化图片工具条，增加自定义功能。
+ [修复] 修复端口冲突问题、pg/mysql未初始化问题。

## v0.2.1 - 2026-06-03

+ [新增] 新增文档站点页面。
+ [优化] 优化画布连线交互。
+ [优化] 优化模型选择用户偏好。

## v0.2.0 - 2026-06-01

+ [新增] 支持通过火山方舟AgentPlan接入。
+ [新增] 视频生成支持声音、水印及图片/视频/音频参考输入。
+ [新增] 画布新增音频节点。
+ [优化] 图片/视频素材支持 `图片1`编号注入提示词。

## v0.1.1 - 2026-05-30

+ [新增] 支持New API跳转并自动填入Base URL和API Key配置。

## v0.1.0 - 2026-05-26

+ [优化] 优化我的画布、我的素材导出功能
+ [修复] 修复画布撤销，配置节点等bug问题

## v0.0.9 - 2026-05-26

+ [新增] 新增视频创作台页面。
+ [修复] 修复图片节点size参数传递问题。

## v0.0.8 - 2026-05-24

+ [新增] 新增用户账号与算力点体系，支持账号密码注册登录、Linux.do OAuth。
+ [新增] 管理后台公开配置支持设置模型算力点、支持计费查询。
+ [新增] 画布右上角展示用户算力点余额，生成按钮会展示本次预计消耗算力点。
+ [新增] 新增视频生成节点。

## v0.0.7 - 2026-05-23

+ [新增] 管理后台提示词管理支持多选批量删除。
+ [新增] 新增定义拉取GitHub提示词源功能。
+ [新增] 新增awesome-gpt-image2-prompts提示词来源。
+ [优化] 优化模型下拉选择样式、优化生图编辑设置

## v0.0.6 - 2026-05-22

+ [新增] 管理后台支持配置模型渠道，前端当前无需鉴权即可直接使用后端渠道能力。
+ [优化] 统一整理后端错误提示、AI 代理、图片节点生成与重试、参考图缺失处理等细节。
+ [优化] 后端模型代理路径调整为 OpenAI 风格。

## v0.0.5 - 2026-05-20

+ [新增] 右上角版本号支持点击查看版本更新弹窗，展示当前版本、最新版本和按时间线整理的更新日志。
+ [新增] 设置弹窗支持配置系统提示词，AI 生图、编辑图和文本请求会自动携带。

## v0.0.4 - 2026-05-20

+ [调整] Docker 运行入口改为 Next.js 对外提供页面，`/api/*` 由 Next.js 代理到内部 Go 服务。
+ [修复] 文本复制在局域网 IP 访问时可能失败的问题。

## v0.0.3 - 2026-05-19

+ [修复] 更新 nanoid 依赖并修改 ID 生成方式，防止其他ip无法使用crypto模块导致的ID生成失败问题。

## v0.0.2 - 2026-05-19

+ [新增] 增加生图工作台功能，支持文生图、图生图、查看历史记录，并增加移动端适配。
+ [修复] 画布生成尺寸控件支持选择更多常用比例，并可直接输入自定义比例。
+ [修复] 生成配置节点恢复拖拽操作，避免面板控件拦截整块节点拖动。
+ [文档] 增加 Render 部署说明。

## v0.0.1 - 2026-05-19

+ [新增] 首次开源版本，包含无限画布能力：多画布项目、节点拖拽缩放、连线、小地图、撤销重做、导入导出。
+ [新增] AI 创作能力：支持 OpenAI 兼容接口的文生图、图生图、参考图编辑和文本问答。
+ [新增] 画布助手能力：支持围绕选中节点和上游节点对话、生图，并把结果插回画布。
+ [新增] 提示词库能力：抓取多个 GitHub 开源项目，按案例整理数百个图片提示词。
